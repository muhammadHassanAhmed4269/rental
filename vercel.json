{
  "version": 2,
  "builds": [
    {
      "src": "client/src/index.js",
      "use": "@vercel/static-build"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "client/build/$1"
    }
  ],
  "build": {
    "commands": {
      "build": "npm install && npm run build"
    }
  }
}
